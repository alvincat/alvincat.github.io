---
title: "二叉搜索树"
date: 2018-05-24 20:41:31
description: "博客摘要：本文主要总结了与二叉查找树相关的知识点。"
categories: ["算法"]
tags: ["二叉树", "查找算法"]
layout: true
top: 
---

# 二叉搜索树
## 二叉搜索树(二叉查找树/二叉排序树)(Binary search Tree)

**定义**：一棵二叉树，若满足如下条件，即任何结点的键值一定大于其左子树中的每一个结点的值，并小于其右子树中的每一个结点的值。则该二叉树为二叉搜索树。<u>二叉搜索树提供对数时间的插入和访问效率。</u>

**最小值**：二叉搜索树中最左侧的结点，即为最小值。即沿根结点一直递归访问左子树，直到树的末端，最末端的结点即为所求。
**最大值**：二叉搜索树中最右侧的结点，即为最大值。原理同上。
**插入**操作: 插入新元素时,可从根结点开始,若新元素键值比当前结点的键值大,则进入当前结点的左子树;若新元素的键值比当前结点的键值大,则进入当前结点的右子树。如此递归，直到尾端，即为插入点。
**删除**操作: 删除操作分为两种情况，情况如下：

1. 若待删除结点A只有1个子结点，则直接将A的子结点连接至A结点的父节点，并将A结点删除即可。
2. 若待删除结点A有两个子结点，则以右子树内的最小结点取代A结点即可。

## 平衡二叉搜索树(balanced binary search tree)

定义：若一个二叉搜索树是平衡的，则该二叉树为平衡二叉搜索树。“平衡”的大致意义是没有任何一个结点过深。不同的平衡条件，具有不同的效率。根据平衡标准的不同，有AVL-tree,RB-Tree,AA-tree树。

## 自平衡二叉搜索树(AVL-tree,Adelson-Velskii-Landis tree)

**定义**：若一棵二叉搜索树中，任何一个结点的左子树的高度与右子树的高度之差的绝对值小于等于1，则这颗二叉搜索树为自平衡二叉搜索树。

### AVL-tree平衡调整

当插入一个结点导致AVL树不平衡时。我们需要找到平衡状态被破坏的各个结点中最深的那个结点。我们将该结点记为X。

方法，根据插入点所处位置的结构特点分为如下4种情形：

1. 插入结点位于X的左结点的左子树---左左
2. 插入结点位于X的左结点的右子树---左右
3. 插入结点位于X的右结点的左子树---右左
4. 插入结点位于X的右结点的右子树---右右

说明：
> + 情况1,4彼此对称，称为外侧(outside)插入，可以使用单旋转(single rotation)操作调整解决。
> + 情况2,3彼此对称，称为内侧(inside)插入，可以使用双旋转(double rotation)操作调整解决。

### 单旋转(single rotation)
　　对于以上的情形1和情形4，可以使用单旋转实现平衡调整。如下图左-左的情形：
![AVL_01](/image/AVL_01.jgp)
调整过程：对于以X为头结点的子树，将XL结点连同其左子树上移，XL作为这棵子树的根结点；X结点(及其右子树)作为XL结点的右结点；XLR结点及其子树作为X结点的左子树。

如下图右-右情形：
![AVL_02](/image/AVL_02.jgp)
调整过程：对于以X为头结点的子树，将XR结点连同其右子树上移，XL作为这棵子树的根结点；X结点(连同其左子树)作为XR左子结点；XRL结点连同其子树作为X结点的右子树。
### 双旋转(double rotation)

对于以上情形的2,3，可以使用双旋转实现平衡调整。例如下图所示的左-右情形：
![AVL_03](/image/AVL_03.jgp)
![AVL_04](/image/AVL_04.jgp)

调整过程：左右结构的情形分为两步完成。
第1步：将XLR(连同其右子树)作为X结点的左子结点，XLR的左孩子(及其子树)作为XL结点的右孩子；XL结点(连同其子树)作为XLR结点的左孩子。
第2步：经过第1部操作以后，得到类似于情形1的结构。在调整后的结构中，将X结点看做该子树的根结点。在此基础上，将当前XL结点(连同其左子树)作为该子树的根结点，当前XL结点的右孩子作为X结点的左孩子；X结点(连同其子树)作为当前XL结点的右孩子。

而对于下图的右-左情形，采用类似的调整方法：
![AVL_05](/image/AVL_05.jgp)
![AVL_06](/image/AVL_06.jgp)
调整过程：右左结构的情形分为两步完成。
第1步：将XRL结点(连同其左子树)作为X结点的右孩子，将XRL结点右孩子(及其子树)作为XR结点的左孩子；XR结点(连同其子树)作为XRL结点右孩子。
第2步：经过第1部操作以后，得到类似于情形4的结构。在调整后的结构中，将X结点看做该子树的根结点。在此基础上，将当前XR结点(连同其右子树)作为该子树的根结点，当前XR结点的左孩子作为X结点的右孩子；X结点(连同其子树)作为XL结点的左孩子。

## 红黑树(RB-Tree,Red-Black Tree)
红黑树是一棵平衡二叉查找树，其定义如下：
1. 每个结点不是红色就是黑色；
2. 根结点为黑色；
3. 如果结点为红色，其子结点必须为黑色(父子结点不能同时为红)；
4. 任一结点至NULL(树尾端)的任何路径，所含黑结点必须相同。
5. NULL结点为黑色
  调整条件：根据规则4，新结点必须为红色；根据规则3，新结点的父节点必须为黑色。当一个新结点根据二叉查找树的规则到达其插入点时，若未能符合上述条件时，就必须调整颜色并旋转树形。
  
